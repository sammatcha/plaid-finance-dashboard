{
  "name": "plaid-finance-dashboard",
  "private": true,
  "version": "0.1.0",
  "description": "Plaid-powered personal finance dashboard.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
